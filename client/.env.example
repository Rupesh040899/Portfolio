# Base URL for the API. Leave blank to use the Vite dev proxy (/api).
VITE_API_URL=

# When true, the client uses built-in mock data instead of the API.
# Handy before your database is connected. Set to "false" once the
# backend + Mongo are live.
VITE_USE_MOCKS=true
